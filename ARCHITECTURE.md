# Kids Code Ops 架构解读

> 本文基于当前仓库内容（主要是 `README.md`）对项目整体架构进行抽象梳理。

## 1. 整体架构风格

项目定位为**面向少儿编程机构运营管理的 Monorepo**，整体采用：

- **后端微服务架构**（Spring Boot + Spring Cloud）
- **多端前端分层**（Admin / Teacher / Mini Program）
- **统一基础设施中台**（MySQL、Redis、Nacos、RabbitMQ、MinIO）

从职责拆分看，这是典型的「**领域服务化 + 多角色端应用**」架构：

- 业务域（学生、课程、财务、成长档案）拆成独立服务；
- 入口统一走网关；
- 通过注册中心、消息中间件和对象存储形成通用能力底座。

## 2. 仓库模块分层

按目录可分为 4 层：

1. **服务层（backend）**：承载核心业务逻辑与数据能力。
2. **交互层（web-admin / web-teacher / mini-parent）**：按用户角色分离界面与流程。
3. **文档层（docs）**：沉淀 OpenAPI、架构图、论文等设计资产。
4. **运维层（scripts）**：存放 SQL/Shell 自动化脚本。

这种分层使得团队可以并行开发：

- 后端团队以服务为单位演进；
- 前端团队按端应用独立发布；
- 运维与文档支撑研发全过程。

## 3. 后端微服务拓扑

### 3.1 入口与治理

- `gateway`（8080）作为**统一流量入口**，负责路由转发与边界控制。
- `auth-service`（8081）提供认证授权（OAuth2/JWT）。
- `rbac-service`（8082）负责用户、角色、权限模型。

这一组合说明系统在架构上强调「先鉴权、后访问」：

1. 客户端请求进入网关；
2. 认证与令牌校验由认证体系处理；
3. 业务服务按 RBAC 权限执行最小授权访问。

### 3.2 业务域服务

- `student-service`：学员主数据与生命周期管理。
- `course-schedule-service`：课程、班级、课表、排课冲突等。
- `finance-service`：订单、支付与财务核算。
- `work-growth-service`：作业、作品、成长档案。
- `notify-service`：消息触达（短信/邮件/微信）。
- `analytics-service`：报表分析与运营指标。
- `file-service`：统一文件上传/存储抽象层（OSS/MinIO）。

从建模角度，这是一种偏 DDD 的按核心子域拆分方式：

- 高频交易链路（课程、订单）独立扩展；
- 通用能力（通知、文件）下沉为公共服务；
- 报表服务与在线事务服务分离，避免耦合。

## 4. 前端多端架构

- `web-admin`（3000）：教务与运营后台，强调配置、统计、管理效率。
- `web-teacher`（3001）：教师端，强调课堂与教学流程。
- `mini-parent`：家长端小程序，强调移动场景触达与低学习成本。

这是典型的「**同后端、异前台**」模式：

- 后端共享统一领域能力；
- 各端按角色定制体验与权限；
- 通过网关与鉴权实现一致接入。

## 5. 基础设施角色

- **MySQL**：事务性主存储，承载核心业务数据。
- **Redis**：缓存与会话加速，降低数据库热点压力。
- **Nacos**：服务注册发现与配置中心，支持微服务治理。
- **RabbitMQ**：异步解耦与削峰（通知、对账、统计异步任务等）。
- **MinIO**：对象存储，承载作业附件、作品资源、图片文件。

该组合符合中小规模微服务系统的标准基础设施选型：

- 成本可控；
- 生态成熟；
- 支持后续横向扩展。

## 6. 关键调用与数据流（建议心智模型）

可将核心流程抽象为：

1. 用户在前端登录 → 网关 → 认证服务签发令牌；
2. 业务请求携带令牌访问目标服务；
3. 业务服务按需：
   - 读写 MySQL；
   - 使用 Redis 缓存；
   - 发布消息到 RabbitMQ；
   - 上传文件到 MinIO；
4. `analytics-service` 汇聚数据形成运营报表；
5. `notify-service` 在关键事件后触达用户。

## 7. 当前仓库可见性与建议

当前仓库主要展示了**架构蓝图与启动规划**，尚未看到各模块源码目录与实现细节。

若后续补齐源码，建议优先补充以下文档以提升可维护性：

- 每个服务的 API 契约与上下游依赖图；
- 统一鉴权链路说明（网关过滤器、JWT 结构、权限点模型）；
- 异步事件清单（事件名、生产者、消费者、重试/幂等策略）；
- 数据库分库分表或多租户策略（如有）；
- 灰度发布与故障演练手册。

---

如果你愿意，我下一步可以基于你后续提供的 `backend/` 真实代码，继续做一版：

1) 按模块类图/包图级别的架构拆解；
2) 关键接口调用链（登录、报名、排课、缴费、通知）时序图；
3) 识别潜在的耦合点与重构建议（含优先级）。
